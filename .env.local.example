# No NEXT_PUBLIC_* env vars needed.
# All API URLs use relative paths resolved by the browser at runtime.
# hyadmin-api: /hyadmin-api (hardcoded in src/lib/api.ts)
# Module APIs: from module.api_url in DB (e.g. /hycert-api)
